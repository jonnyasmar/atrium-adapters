#!/usr/bin/env bash
# backup-paths.sh — Tilde-prefixed paths the pi install/uninstall
# touches. test-adapter.sh backs these up before testing and restores
# on exit.
echo "~/.pi/agent/extensions/atrium.ts"
