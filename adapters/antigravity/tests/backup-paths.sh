#!/usr/bin/env bash
# backup-paths.sh — Tilde-prefixed paths the agy install/uninstall touches.
# test-adapter.sh backs these up before testing and restores on exit.
echo "~/.gemini/config/hooks.json"
