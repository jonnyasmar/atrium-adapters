#!/usr/bin/env bash
# backup-paths.sh — Tilde-prefixed paths the grok install/uninstall touches.
# test-adapter.sh backs these up before testing and restores on exit.
echo "~/.grok/hooks/atrium-grok.json"
