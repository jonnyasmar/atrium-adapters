#!/usr/bin/env bash
# backup-paths.sh — Tilde-prefixed paths the omp install/uninstall
# touches. test-adapter.sh backs these up before testing and restores
# on exit.
echo "~/.omp/agent/extensions/atrium.ts"
