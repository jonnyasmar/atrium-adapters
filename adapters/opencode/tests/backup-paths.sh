#!/usr/bin/env bash
# backup-paths.sh — Tilde-prefixed paths the opencode install/uninstall
# touches. test-adapter.sh backs these up before testing and restores
# on exit.
echo "~/.config/opencode/plugins/atrium.js"
