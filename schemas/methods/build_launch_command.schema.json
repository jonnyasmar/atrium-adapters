{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "build_launch_command output",
  "description": "Output schema for the build_launch_command method.",
  "type": "object",
  "required": ["command"],
  "properties": {
    "command": {
      "type": "array",
      "items": { "type": "string" },
      "minItems": 1,
      "description": "The command and arguments to launch a new session."
    }
  },
  "additionalProperties": false
}
